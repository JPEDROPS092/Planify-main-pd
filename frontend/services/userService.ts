/**
 * Arquivo de compatibilidade para importações antigas
 * Redireciona para o novo caminho dos serviços de usuários
 */

// Exporta o serviço de usuários do novo caminho
export { useUserService } from './api/userService'

/**
 * Componente LazyWrapper
 *
 * Componente para carregamento preguiçoso de conteúdo.
 */

// Importação direta para evitar duplicação de componentes no registro automático do Nuxt
import LazyWrapper from './LazyWrapper.vue';

// Exportação explícita do componente
export { LazyWrapper };
export default LazyWrapper;

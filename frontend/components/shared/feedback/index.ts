/**
 * Componentes de Feedback
 *
 * Componentes para fornecer feedback visual ao usuário.
 */

// Importação dos componentes e módulos
import * as Loading from './loading';
import * as Notification from '~/components/shared/feedback/notification';

// Exportação explícita dos componentes
export {
  Loading,
  Notification
};

// Exportação padrão para uso com importações default
export default {
  Loading,
  Notification
};

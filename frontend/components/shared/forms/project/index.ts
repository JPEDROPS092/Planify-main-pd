/**
 * Componente ProjectForm
 *
 * Formulário completo para criação e edição de projetos com validação e associação automática ao usuário logado.
 */

// Importação direta para evitar duplicação de componentes no registro automático do Nuxt
import ProjectForm from './ProjectForm.vue';

// Exportação explícita do componente
export { ProjectForm };
export default ProjectForm;

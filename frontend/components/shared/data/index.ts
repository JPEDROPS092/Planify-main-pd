/**
 * Componentes de Dados
 *
 * Componentes para exibição e manipulação de dados.
 */

// Importação dos componentes e módulos
import * as DataTable from './data-table';
import * as EmptyState from './empty-state';
import * as TableSkeleton from './table-skeleton';

// Exportação explícita dos componentes
export {
  DataTable,
  EmptyState,
  TableSkeleton
};

// Exportação padrão para uso com importações default
export default {
  DataTable,
  EmptyState,
  TableSkeleton
};

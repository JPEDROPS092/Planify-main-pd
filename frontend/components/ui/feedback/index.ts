/**
 * Componentes de Feedback
 *
 * Componentes UI para fornecer feedback visual e informações ao usuário.
 */

// Importação dos componentes por categoria
import * as ActivityFeed from './activity-feed';
import * as Badge from './badge';
import * as MetricsCard from './metrics-card';

// Exportação explícita dos componentes
export {
  ActivityFeed,
  Badge,
  MetricsCard
};

// Exportação padrão para uso com importações default
export default {
  ActivityFeed,
  Badge,
  MetricsCard
};

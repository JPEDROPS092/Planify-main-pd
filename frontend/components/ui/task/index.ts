/**
 * Componente TaskForm
 *
 * Este componente fornece um formulário completo para criação e edição de tarefas
 * com validação em tempo real, auto-save e upload de arquivos.
 */

// Importação direta para evitar duplicação de componentes no registro automático do Nuxt
import TaskForm from './TaskForm.vue';

// Exportação explícita do componente
export { TaskForm };

/**
 * Componentes de Navegação
 *
 * Componentes UI para navegação e controle de fluxo na interface.
 */

// Importação dos componentes por categoria
import * as Breadcrumbs from './breadcrumbs';
import * as Pagination from './pagination';
import * as TabGroup from './tab-group';

// Exportação explícita dos componentes
export {
  Breadcrumbs,
  Pagination,
  TabGroup
};

// Exportação padrão para uso com importações default
export default {
  Breadcrumbs,
  Pagination,
  TabGroup
};

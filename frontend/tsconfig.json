{
  // Extende as configurações do tsconfig.json gerado pelo Nuxt no diretório .nuxt
  // Isso garante que os tipos e aliases do Nuxt sejam reconhecidos.
  "extends": "./.nuxt/tsconfig.json",
  // Você pode adicionar ou sobrescrever opções do compilador aqui, se necessário.
  // Por exemplo:
  // "compilerOptions": {
  //   "strict": true, // Habilitar verificações mais rigorosas
  //   "noUnusedLocals": true,
  //   "noUnusedParameters": true
  // }
  // No entanto, para resolver problemas de "Cannot find module '#app'",
  // é melhor garantir que o "extends" esteja correto e que o diretório .nuxt
  // seja gerado corretamente (executando 'npm run dev' ou 'npm run build').
  // Manter as "paths" e "baseUrl" gerenciadas pelo Nuxt é geralmente mais seguro.
}
